# =============================================================================
# JobLens — environment template (safe to commit)
# Copy to .env.local and replace placeholders before running locally.
# =============================================================================

# Groq API key for LLM job analysis (server-side only).
# Get one at: https://console.groq.com/keys
# Used by: lib/services/groqService.ts and POST /api/analyze
GROQ_API_KEY=your_groq_api_key_here
